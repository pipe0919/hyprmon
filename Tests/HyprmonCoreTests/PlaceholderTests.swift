import XCTest
@testable import HyprmonCore

final class PlaceholderTests: XCTestCase {
    func testPackageBuilds() {
        // Placeholder test ensures the test target compiles before real tests arrive.
        // Replaced by real tests in later batches.
        XCTAssertTrue(true)
    }
}
